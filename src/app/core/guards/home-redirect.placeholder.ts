import { Component } from '@angular/core';

/** Placeholder; `homeRedirectGuard` siempre redirige antes de renderizar. */
@Component({
  standalone: true,
  template: '',
})
export class HomeRedirectPlaceholder {}
